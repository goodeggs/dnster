{
  "name": "dnster",
  "version": "0.0.1",
  "dependencies": {
    "async": "*",
    "http-proxy": "*",
    "optimist": "*"
  },
  "devDependencies": {
    "coffee-script": "*",
    "chai": "*",
    "mocha": "*",
    "sinon": "*",
    "sinon-chai": "*",
    "tmp": "*"
  },
  "bin": {
    "dnster": "./bin/dnster.js"
  },
  "scripts": {
    "compile": "./node_modules/.bin/coffee -c -o lib src",
    "prepublish": "npm run compile",
    "pretest": "npm run compile",
    "test": "./node_modules/.bin/mocha"
  }
}
